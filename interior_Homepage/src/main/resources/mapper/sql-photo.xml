<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.big.mapper.PhotoMapper">

    <!-- 사진 추가 -->
    <insert id="insertPhoto" parameterType="org.big.dto.PhotoDto">
        INSERT ALL
      <foreach collection="list" item="item" index="index" open=""
         separator=" " close="SELECT * FROM DUAL">

         INTO in_photos
         (
        	*
         )
         VALUES
         (
         extractvalue(dbms_xmlgen.getxmltype('select tfile_seq.nextval - 1 from dual'),'//text()'),
         #{item.iphotoId},
         #{item.iphoto_project_id},
         #{item.iphoto_review_id},
         #{item.iphoto_url},
         #{item.iphoto_description},
         )
      </foreach>

    </insert>

    <!-- 특정 리뷰에 연결된 사진 목록 조회 -->
    <select id="getPhotos" parameterType="int" resultType="org.big.dto.PhotoDto">
        SELECT
            iphoto_id AS iphotoId,
            iphoto_review_id AS iphotoReviewId,
            iphoto_url AS iphotoUrl,
            iphoto_description AS iphotoDescription
        FROM in_photos
        WHERE iphoto_review_id = #{reviewId}
    </select>

    <!-- 특정 리뷰에 연결된 사진 삭제 -->
    <delete id="deletePhotos" parameterType="int">
        DELETE FROM in_photos
        WHERE iphoto_review_id = #{reviewId}
    </delete>

</mapper>
